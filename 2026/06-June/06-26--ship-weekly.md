# ship · 0615–0626

日期: 2026年6月26日
標籤: weekly

这两周像是「先交一个、再开两个」的节奏。前半段还在跟万益特的装柜逻辑死磕，后半段一口气把 2.0 交付了，紧接着又接了两个新坑——一边收尾一边开新摊子，挺充实的。

## 🧩 主线

### 万益特：从「想清楚」到「交付」

- 前半段重构混装逻辑——改成打竖优先，柜宽方向空隙太大就打横装一列，40 尺和 20 尺分开想。最关键的是终于想明白核心其实是「每种货品在每种柜子里每行怎么摆」，先把框架搭好，剩下的按客户规则调层高和行数就行。
- 后半段动手，40 尺 + 20 尺拼柜逻辑写完，**2.0 交付，现在等客户反馈**，1.0 开了个新分支存档。一句话提醒自己：工作是边做边推进的，先做起来才知道哪里想错了。

### 又开两个新坑

- **中石油化销广州的 OCR & RPA**：识别图片 → 打 tag → 特定 tag 拼图 → RPA 在客户系统上传拼好的图。目前只梳理了需求，还没动代码。
- **风控那条线**：在原来合同抓取的 RPA 上加了个新需求——算审批时长、末尾加一列。

### 工具链打通

- 这两周最爽的一件事：Notion 真的连上 GitHub 了（折腾出令牌才成），现在能让 Notion AI 直接进库里改代码，`git fetch` 只读拉、`git checkout` 切到 sales。再加上装了 Notion 官方 MCP——以前在 IDE / 浏览器 / Notion / GitHub 之间来回切，现在信息能双向流动了。

## 🧠 学到 / 想明白的

- 捡起 **Anki** 背单词，把英文一手资料里的生词例句丢给 AI 整理成可导入格式，挺顺手。
- 让 AI 讲了 **Evals**——给 AI 输出「打分、做体检」的一套方法，相当于 AI 版的单元测试；但因为输出是不确定的，没法用老办法测。
- 顺手补了点算法（哈希表），整理了个刷题路线页面。
- 又冒出「把 OCR 封装成库」的念头，以后直接调用。

## 💡 杂七杂八

- 这两周最大的体感：真正有考验性的是**梳理需求和测试调试**，代码让 AI 协作十几分钟、几十分钟就写完了。感觉自己正在从「写代码的人」慢慢变成「定义问题、编排 AI 的人」。
- 「让子弹飞一会」又用上了——装柜逻辑没想透之前不急着动手，框架想明白了再让 code agent 落地。
- 工具链每打通一格，「想」和「做」之间的距离就短一截，值得花时间把这条链路用熟。