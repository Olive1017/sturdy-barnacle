# upload · 0706–0710

日期: 2026年7月10日
標籤: weekly

这周是「一件事往深里做」的节奏。上周开的中石油化销那个坑，这周从拼图收尾一路推到 RPA 上传和整体架构，基本一个人闷头把主线往前拱了一大截。

## 🧩 主线

### 中石油化销：从「拼图」到「上传 + 工程化」

- 前半段收尾照片识别和拼图：调提示词治幻觉，分清了哪些幻觉能靠提示词规避、哪些只能靠后续校验和人工兜底兜住。拼图模块基本做完，核心图片处理能力落地。
- 然后重心转到上传——对照片录入系统做了初步录制并生成代码，把自动化上传的路子先跑通了一遍。
- 后半段一直在梳理 RPA 上传逻辑和整体架构，跟 AI 反复对开发思路，把「识别 → 拼图 → 上传」这条链路的工程化框架往清楚里想。

### 识别结果可视化

- 开始给识别结果加表格 / UI 展示，让每一步的结果能被看到、被确认，也给后面的人工兜底留了口子。

## 🧠 学到 / 想明白的

- 搭了个 AI / 软件工程的**阅读清单**，按四个能力分类：验收深度 / 隐性上下文（FDE）/ 定义问题与品味 / 系统性编排，配合 AI 辅助阅读。
- 读了《AI 把编程这件事接管之后，作为程序员的我该何去何从？》等几篇文章，加上科技周刊，持续补 AI 工程这条线的输入。
- 顺手厘清了两个小概念：`origin` 不是命令关键字而是远程仓库地址的**别名**；`field` 是「存下来的事实」，`property` 是「每次现算的推导」——一个是我拥有的，一个是我算出来的。

## 💡 杂七杂八

- 这周体感最强的还是那句老话：真正吃功夫的是**梳理需求和调试兜底**，代码交给 AI 十几分钟就出来了。「定义问题、编排 AI」这个角色这周又坐实了一点。
- 识别这块的幻觉提醒自己：LLM 的输出天然不确定，与其追求「一次全对」，不如把校验和人工兜底做进流程里，让系统整体可靠。
- 项目状态盘一下：拼图 ✅ 可用；正在推进 RPA 上传、整体架构、结果可视化。下周该动手把上传真正跑起来了。