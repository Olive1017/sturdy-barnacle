# 0713 Mon

日期: 2026年7月13日
標籤: daily

- [任务]
    - 壳牌下载（shell_print）提出新需求。现在文件夹内都是以sap单号命名的pdf，现在加一步按车队名字建立子文件夹，把车队对应的订单pdf移动到子文件夹内。
    - 中石油 另外看一下uploader里是不是有要移到config的代码? 开始修正playwright元素定位
- [学习]
    - **函数签名**(function signature)——指一个函数**怎么被调用的那部分定义**:函数名 + 参数(名字、顺序、有没有默认值、类型) + 返回什么。**不含函数体里的代码**,只描述它的"接口长相"。
    - **结构良好的代码,bug 几乎都不在模块内部,而在模块之间的接缝上**(函数签名、属性契约、谁负责给谁赋值、返回值的形状)。看不见接缝的人,就只能在每个文件内部"重写到看起来对为止"。
    - 思考如何教代码助手最小改动纪律（如何在设置里配置一下
    - 以后做浏览器自动化的时候，要复制outerHTML，因为它会给出这个元素**完整的标签+所有属性**(id、class、src、alt、placeholder…)。再发给ai,就能挑一个**最稳定**的定位方式(优先级:id > name/placeholder > 稳定的 class > 文本)
- [7788]
    -